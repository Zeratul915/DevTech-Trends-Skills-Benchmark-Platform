# 07 可视化模块学习

## 对应类

- `Visualizer`

## 类职责

`Visualizer` 负责把分析结果变成图表 HTML，包括柱状图、折线图、词云和个人技能雷达图。

## 降级设计

如果本地安装了 pyecharts，就生成正式图表。如果没有安装，也会返回简单 HTML 列表，保证演示不因为环境问题中断。

## 汇报重点

可视化模块只关心“怎么展示”，不关心数据来自哪里。
