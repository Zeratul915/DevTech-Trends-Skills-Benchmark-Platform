# 06 数据分析模块学习

## 对应类

- `Analyzer`

## 类职责

`Analyzer` 负责把数据库查询结果变成统计结果，例如语言排行、关键词词频、趋势序列、热门技能分数和个人技能差距。

## 设计原因

分析模块不写数据库、不生成页面，只处理数据。这使得同一个分析结果可以给 Web 页面、报告或图表模块复用。

## 汇报重点

`Analyzer` 是“数据到结论”的转换层，是数据库和可视化之间的桥梁。
