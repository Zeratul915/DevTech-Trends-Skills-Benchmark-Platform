# 04 数据清洗模块学习

## 对应类

- `RegexCleaner`
- `SkillExtractor`

## 类职责

`RegexCleaner` 封装通用正则能力，例如清理 HTML 标签、解析 `1.2k` 这种 star 数、提取版本号。

`SkillExtractor` 负责从仓库名称、描述和语言中识别技术关键词，例如 Python、Flask、Scrapy、SQLite。

## 正则表达式落地

项目中的正则不是孤立练习，而是直接用于把网页文本转换成结构化数据。

## 汇报重点

清洗逻辑独立成类以后，爬虫只负责拿网页，清洗类负责把脏文本变干净。
